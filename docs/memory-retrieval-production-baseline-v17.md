# Memory Retrieval Production Baseline v17

## Purpose

`memory-retrieval-production-v17` certifies the production-source delta for
Dashboard Batch 6B-2B.1 cancellation-boundary hardening. It is not a Memory
Retrieval algorithm change. The parent is
`memory-retrieval-production-v16`, and every v1-v16 manifest remains immutable.

Manifest:
`tests/fixtures/memory_retrieval_production_freeze/v17.json`.

Manifest SHA-256:
`2ac1d7185488dd1008407e4711fc3777213dcc1cd405e104f44bf6ca20206857`.

## Exact v16 to v17 lineage

Changed protected files:

- `minicode/conversation.py`
- `minicode/conversation_turn_store.py`
- `minicode/web/static/assets/app.js`

Added protected files: none.

Removed protected files: none.

Every change uses reason code
`dashboard_chat_cancellation_boundary_hardening` and the single reason
`Batch 6B-2B.1 cancellation boundary hardening`. The protected set remains 33
files.

## Verification properties

- The default verifier is read-only and selects v17 as the active baseline.
- Candidate construction requires v16 as the parent and rejects any changed,
  added, or removed path outside the exact declaration above.
- The verifier validates deterministic candidate equality, all 33 current
  source hashes, every historical manifest pin, and both the v15→v16 and
  v16→v17 lineage contracts.
- v16 writing is validation-only once its fixed manifest exists. Only the fixed
  canonical v17 target can be generated by the v17 writer.
- Tamper tests cover the changed Store, Conversation, and formal JavaScript
  contracts and identify the exact changed path without rewriting a manifest.

Final verification reports `candidateMatches=true`, 33/33 current protected
files, and `manifestIntegrity=true` for every version v1-v17. The baseline test
matrix passed 83 tests; the combined active semantic contract passed 84 tests.

## Semantic preservation

The official offline evaluator remains unchanged:

- 108 cases;
- 37 confirmed gaps;
- zero remote calls;
- phase3b gate true;
- `evaluation_passed=true`.

The accepted semantic gold remained byte- and metadata-identical before and
after evaluation:

- SHA-256
  `5629d6cfc5d2573d37a348ff08909706fdc6a8ad65082b86e3cb0005de1fdd3b`;
- size `3,033,592` bytes;
- mtime ns `1784135857000000000`.

Generated evaluator reports remain separate from the accepted artifact.
