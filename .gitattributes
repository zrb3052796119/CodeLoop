# Byte-level release manifests require one text checkout form on every runner.
# Windows launch shims retain their native line endings, and image bytes must
# never pass through text normalization.
* text=auto eol=lf
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.png binary
*.jpg binary
