# Frozen quality fixtures use content hashes and must retain one checkout form
# on every CI runner. Keep this rule narrow: Windows launch shims and unrelated
# project files should continue to use their repository/platform defaults.
tests/fixtures/agent_quality/*.json text eol=lf
