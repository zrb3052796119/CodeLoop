# 2026-04-09 Working Memory

## P6: USER.md User Profile System — COMPLETED

Implemented the full USER.md user profile system for MiniCode Python (py-src/minicode/).

### Files created:
- `user_profile.py` — Core module with UserProfile/UserPreferences/CodingStyle dataclasses, Markdown parser/serializer, UserProfileManager (global+project merge), handle_user_command() for CLI

### Files modified:
- `config.py` — Added MINI_CODE_USER_PROFILE_PATH, project_user_profile_path(), responseLanguage/responseVerbosity fields in load_runtime_config(), profile info in format_config_diagnostic()
- `prompt.py` — Injects user profile section into system prompt (after CLAUDE.md, before memory)
- `cli_commands.py` — Added /user command with subcommands: show, global, project, paths, reset, reset-global, set, search
- `main.py` — Initializes UserProfileManager at startup
- `context_collector.py` — Added ContextType.USER_PROFILE, ContextData.user_profile field, async collection
- `.env.example` — Added MINI_CODE_LANGUAGE, MINI_CODE_VERBOSITY env vars

### Key design decisions:
- USER.md is Markdown (not JSON) for human readability/editability
- Two scopes: global (~/.mini-code/USER.md) + project (.mini-code/USER.md)
- Project overrides global for conflicting fields, inherits for missing ones
- Profile injected into system prompt automatically via prompt.py
- 0 lint errors, all smoke tests passed

## P5 (completed previously): Multi-model switching with OpenRouter/custom endpoint support
