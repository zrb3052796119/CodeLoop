# 2026-04-10 Working Memory

## MiniCode py-src 代码库瘦身重构

### 背景
代码库膨胀到 86 个文件 / 916KB，大量 "Inspired by Claude Code" 的架构模块是孤岛（建了框架但没接入主流程）。

### 删除的死代码模块（11个，共 ~208KB）
- `sub_agents.py` (15KB) — 子代理系统框架，零引用
- `gateway.py` (18KB) — API 网关，零引用
- `async_context.py` (8KB) — 异步上下文，零引用
- `context_collector.py` (16KB) — 上下文收集器，零引用
- `cost_integration.py` (7KB) — 成本集成，零外部引用
- `cron_runner.py` (12KB) — 定时任务，零引用
- `poly_commands.py` (13KB) — 多态命令，import 但未实际使用
- `task_tracker.py` (13KB) — 任务追踪，只被 poly_commands 引用
- `tooling_enhanced.py` (22KB) — 增强工具框架，只被 cost_integration 引用
- `governance_audit.py` (14KB) — 治理审计主模块，零引用
- `state_integration.py` (6KB) — 功能合并到 state.py

### 删除的半成品 tools（7个，共 ~62KB）
- `db_explorer.py` (12KB) — 仅 SQLite，未验证
- `docker_helper.py` (12KB) — Docker 操作，未验证
- `api_tester.py` (10KB) — 与 web_fetch 重叠
- `notebook_edit.py` (6KB) — Jupyter 编辑，未验证
- `run_with_debug.py` (10KB) — 与 run_command 重叠
- `governance_audit_tool.py` (1KB) — wrapper，主模块已删
- `multi_edit.py` (5KB) — 与 edit_file/patch_file 重叠

### 合并操作
- `state_integration.py` → 合并入 `state.py`（全局单例 + handle_state_command）
- `cli_commands.py` 移除了对 `poly_commands` 的假引用
- `cli_commands.py` 的 `/state` 命令改为引用 `state.handle_state_command`

### Bug 修复
- `skills.py` 的 `_list_skill_dirs()` 添加了 OSError try-except，修复 Windows 上不受信任的装入点导致崩溃的问题

### 结果
- 重构前：86 个文件，916KB，28 个工具
- 重构后：68 个文件，708KB，22 个工具
- 减少：18 个文件，~208KB（-23%）
- 所有核心 import 链验证通过
- Tool registry 创建正常（22 个工具注册成功）

### 2026-04-10 下午优化（第二轮）
**清理未使用的 import：**
- `tty_app.py` 移除了 `list_background_tasks` 的未使用 import（但保留 background_tasks 模块因为 run_command.py 仍使用）

**删除 platform_adapters.py (31KB)：**
- 完整的平台适配器框架（支持 Telegram/Discord/Slack），但从未被引用
- 这是一个"看起来很美但没用过"的模块

**代码库最终状态：**
- 67 个文件，676KB，22 个工具
- 所有剩余模块都有实际引用，无死代码

**本轮增强功能检查：**
- MCP (mcp.py 32KB)：已有超时控制、重试机制、错误处理，不需要增强
- context_manager (34KB)：功能完整，代码结构合理，不需要精简
- platform_adapters：已删除

### 2026-04-10 第三轮：交互模块优化
**tty_app.py 未使用 import 清理（~18 个）：**
- `run_agent_turn` — 未调用
- `find_matching_slash_commands` — 未调用
- `save_history_entries` — 未调用
- `parse_local_tool_shortcut` — 未调用
- `build_system_prompt` — 未调用
- `AppState`, `Store`, `format_app_state_summary` — 未使用
- `render_banner`, `render_footer_bar`, `render_panel`, `render_slash_menu`, `render_status_line`, `render_tool_panel` — 未调用
- `clear_screen`, `get_transcript_window_size`, `render_transcript` — 未调用
- `AggregatedEditProgress` — 未使用
- `render_input_prompt` — 未调用

**代码库最终状态：**
- 67 个文件，676KB，22 个工具
- 所有剩余模块都有实际引用，无死代码

### 2026-04-10 第四轮：性能优化
**token 估算缓存：**
- 添加 LRU 缓存（1024 条目），避免重复计算相同文本
- 长文本使用 hash 作为 key 减少内存占用

**现有性能优化（已确认）：**
- permissions.py：LRU 缓存路径规范化（512 条目）
- tty_app.py：_ThrottledRenderer 节流渲染（16ms 间隔，约 60fps）
- chrome.py：终端大小缓存（0.5s TTL）+ 缓存失效机制
- agent_loop.py：并发工具执行（ThreadPoolExecutor，最多 8 个 worker）

**用户手感改进：**
- 渲染节流从 30fps 提升到 60fps，响应更流畅
- 所有缓存策略减少 CPU 占用

### 2026-04-10 第五轮：能力提升
**新增工具（22 个）：**

数据处理（4）：
- json_format — 格式化 JSON
- json_parse — 解析 JSON + 路径提取
- regex_test — 正则测试
- regex_replace — 正则替换

HTTP（1）：
- http_request — 完整 HTTP 请求

批量操作（3）：
- batch_copy — 复制
- batch_move — 移动
- batch_delete — 删除

编码转换（4）：
- base64_encode — Base64 编码
- base64_decode — Base64 解码
- url_encode — URL 编码
- url_decode — URL 解码

加密与时间（4）：
- current_time — 当前时间
- timestamp_convert — 时间戳转换
- hash — 哈希 (MD5/SHA)
- hmac — HMAC

压缩归档（6）：
- gzip_compress — gzip 压缩
- gzip_decompress — gzip 解压
- tar_create — tar 打包
- tar_extract — tar 解压
- zip_create — zip 打包
- zip_extract — zip 解压

**结果：**
- 工具数：22 → 44 个（+100%）

### 2026-04-10 第六轮：更多能力
**新增工具（8 个）：**

CSV（2）：
- csv_parse — CSV 转 JSON
- csv_create — JSON 转 CSV

文本处理（6）：
- uuid_generate — UUID 生成
- text_sort — 文本排序
- text_dedupe — 去重
- text_join — 行连接
- line_count — 行数统计
- random_string — 随机字符串

**结果：**
- 工具数：44 → 52 个（+18%）

### 性能优化（核心改进）
**ToolRegistry 查找优化：**
- 添加 _tool_index 字典，O(1) 查找代替 O(n) 遍历
- 1000 次查找：0.09ms（极快）

**现有性能优化（已确认）：**
- token 估算 LRU 缓存（1024 条目）
- permissions.py LRU 路径缓存（512 条目）
- tty_app.py 渲染节流（60fps）
- chrome.py 终端大小缓存（0.5s TTL）
- agent_loop.py 并发工具执行（8 workers）
- input_parser.py 预编译正则表达式
- tooling.py 输出智能截断

**最终结果：**
- 代码：76 文件，735KB
- 工具：52 个
- 性能：工具查找 O(1)，渲染 60fps
